<h2><%= @list.name %></h2>
<h5>All the Bookmarks for the list :</h5>
<div>
  <ul>
    <% @bookmarks.each do |bookmark| %>
      <li><p><em><%= bookmark.movie.title %></em> : <%= bookmark.comment %></p> <%=  %></li>
    <% end %>
  </ul>
</div>

<%= link_to "Add a bookmark to the list", new_list_bookmark_path(@list) %>
